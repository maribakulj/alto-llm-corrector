# Workspace root — uv.
#
# One member since the demo moved out on 2026-08-16. It stays for as
# long as `packages/lidenbrock/` is a subdirectory rather than the
# repository root; flattening the tree retires this file with it.

[project]
name = "lidenbrock-workspace"
version = "0.0.0"
description = "Workspace root — not published"
requires-python = ">=3.11"

[tool.uv.workspace]
members = [
    "packages/lidenbrock",
]
